<workflow-app name="BI Data Ingestion" xmlns="uri:oozie:workflow:0.5">
    <start to="shell-26f4"/>
    <kill name="Kill">
        <message>Action failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <action name="shell-26f4">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <exec>01-preprocess_links_app.sh</exec>
            <capture-output/>
        </shell>
        <ok to="shell-7a2b"/>
        <error to="Kill"/>
    </action>
    <action name="shell-7a2b">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <exec>02-source_data_to_parquet_app.sh</exec>
            <capture-output/>
        </shell>
        <ok to="shell-d8a6"/>
        <error to="Kill"/>
    </action>
    <action name="shell-d8a6">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <exec>03-link_data_app.sh</exec>
            <capture-output/>
        </shell>
        <ok to="fork-ca1c"/>
        <error to="Kill"/>
    </action>
    <action name="shell-5578">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <exec>04-load_bi_to_es_app.sh</exec>
            <argument>${elastic}</argument>
            <argument>${index}</argument>
            <capture-output/>
        </shell>
        <ok to="join-0031"/>
        <error to="Kill"/>
    </action>
    <action name="shell-f212">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <exec>04-hmrc_csv_app.sh</exec>
            <capture-output/>
        </shell>
        <ok to="join-0031"/>
        <error to="Kill"/>
    </action>
    <fork name="fork-ca1c">
        <path start="shell-5578" />
        <path start="shell-f212" />
    </fork>
    <join name="join-0031" to="End"/>
    <end name="End"/>
</workflow-app